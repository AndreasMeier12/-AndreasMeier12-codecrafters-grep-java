package tester;

import matcher.IMatcher;

import java.util.List;

public class Tester {
    public static boolean test(List<IMatcher> matchers, String a){
        int matcherPos = 0;
        int stringPos = 0;

        while (stringPos < a.length() && matcherPos < ){
            IMatcher matcher = matchers.get(matcherPos);



        }

        return true;


    }

}
